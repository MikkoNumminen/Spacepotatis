"use client";

// HUD — heads-up display: health, shield, score, current weapon, credits.
// Subscribes to GameState via useSyncExternalStore.
//
// Implementation deferred to Phase 4.3 (see TODO.md).

export default function HUD() {
  return null;
}
