# AI-codegen smell audit — 2026-05-31

## Run 1 (22:38 UTC)

**Scope:** `git diff 9af858a..HEAD` source files (51 `*.ts`/`*.tsx`, tests excluded) — the churn since the modular-architecture audit close (#254–#275).
**Checks run:** 9 of 10 (check #7 mirror-tests is test-only; default scope excludes tests).
**Total findings:** 1 (0 major, 1 minor, 0 nit)

### Findings

| Check | Severity | File:line | Snippet | Suggested action |
|-------|----------|-----------|---------|------------------|
| swallowed-errors | minor | src/lib/saveValidation.ts:657 | `} catch { continue; }` | Add a one-line comment naming the safe-to-ignore condition, OR factor the `try { getEnemy } catch { continue }` pattern (shared with :186) into a `getEnemyOrSkip` helper so the rationale lives once. |

### Grouped by severity

**Minor findings:**
- [swallowed-errors] `src/lib/saveValidation.ts:657` — bare `catch { continue }` in the theoretical-max score loop. Its structural twin at `:186` does the same `getEnemy(spawn.enemy)`-in-a-loop guard but carries the explaining comment ("Wave references an enemy id we no longer recognise. Skip rather than throw — data integrity is its own test layer."). :657 recovers identically (skips the unknown enemy) but has no comment, so a reader at the second site can't see why the throw is safe to eat. Pre-existing code; surfaced because saveValidation.ts is in the churn set. Low blast radius — the swallowed throw is `getEnemy` on a drifted id, which the schema/integrity tests already gate.

### Out-of-band observation (not one of the 10 checks)

Not a finding under the strict check definitions — recorded for the human:

- **Repeated panel/heading shell.** The catalog-panel chrome `className="rounded border border-space-border bg-space-panel/70 p-4 sm:p-5"` + `<h2 className="mb-4 font-display tracking-widest text-hud-green">` repeats verbatim across the three new `shop/` sections, `Leaderboard`/`TopPilots`, and the leaderboard page. This is **consistent** (not stylistic drift) and it's a Tailwind class string, not a function — so it trips neither #2 (stylistic-drift) nor #9 (duplicated-helpers). But a shared `<CatalogSection title>` / `<Panel>` primitive in `components/ui/` would DRY it and make a future restyle one-touch. Low priority; purely a consolidation opportunity.

### What was checked and came back clean

- **swallowed-errors** — all other catches in scope recover with a fallback, log, rethrow, or carry a why-comment (`save/route.ts:218,282`, `leaderboard/route.ts:84`, `HandlePrompt.tsx:85`, `sync.ts:636`, `useGalaxyScene.ts:127` inner-dispose, `retryWithBackoff.ts:48`, `saveValidation.ts:186,627`). The new retry-loop dispose-on-throw blocks (`useGalaxyScene`/`usePhaserGame`) are documented-why + best-effort, the canonical legit shape.
- **phantom-todos** — zero TODO/FIXME/XXX/HACK without ticket/owner/condition in the churn. (The known `story.ts:51` phantom from the calibration pass is outside this scope — story.ts wasn't touched.)
- **defensive-checks-for-impossible-cases** — the new hooks' `if (canvas)` / `if (!parent)` guards are on `ref.current` (`T | null`) — narrowing-eligible, legit.
- **generic-names-in-domain-context** — `const result =` (validate/retry/flush returns, idiomatic — `result.kind` discriminated union) and `const data = img.data` (ImageData pixel buffer, the explicitly-legit case) only.
- **single-use-helpers** — `resolveCombatTrack` (2 callers), `retryWithBackoff` (2), and the GameCanvas hook/component extractions (`useGalaxyTransition`, `GalaxyStatusOverlays`, `useGameMode`, `useVictoryFlow`, `useTransitionOverlay`) are each called once BY DESIGN — they exist to de-god-file the orchestrator (CLAUDE.md §5), not as premature abstraction.
- **over-typed-primitives** — every `as const satisfies readonly <Id>[]` in `schemas/save.ts` + `types/game.ts` is the canonical legit pattern (pins literals AND verifies Id membership); schemas/types are skip-listed anyway.
- **duplicated-helpers** — the three `shop/` sections share an outer panel shell but differ in param shape, body skeleton, and length (132/89/160 LOC); they do not meet the 1+2+3 near-duplicate bar. See the out-of-band observation for the shell.
- **stylistic-drift-within-file**, **paraphrase-comments** — no matches (Prettier/ESLint-enforced; comments explain why per §5).

### Suppressed by sidecar

Consulted `docs/audits/_dismissals.md` (empty table) — 0 findings suppressed.

**Summary: FAIL (0m 1min 0n)** — one minor swallowed-error (`saveValidation.ts:657`), pre-existing, low blast radius. The reviewer decides whether it blocks; recommend a one-line comment rather than treating it as a merge gate.
