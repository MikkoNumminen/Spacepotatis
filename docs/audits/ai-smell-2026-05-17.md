# AI-codegen smell audit — 2026-05-17

## Run 1 (04:30 UTC)

**Scope:** `src/game/audio/*.ts` (excluding `*.test.ts` per default rule) + `.claude/skills/ai-codegen-smell-audit/*.md` (user-requested inclusion of the skill's own docs).
**Checks run:** 10 (with check #7 mirror-tests dormant — default scope excludes test files; user did not pass `--include-tests`).
**Total findings:** 1 (0 major, 0 minor, 1 nit).

### Findings

| Check | Severity | File:line | Snippet | Suggested action |
|-------|----------|-----------|---------|------------------|
| phantom-todos | nit | src/game/audio/story.ts:51 | `// TODO: when per-category sliders ship in MuteToggle, this engine plays...` | Add a ticket (`#nnn`) and an owner (`@handle`) so future maintainers can track ownership. The condition is present and clear ("when per-category sliders ship") — only the ticket + owner are missing. |

### Grouped by severity

One bullet per finding. Omit a severity section entirely if it has zero findings.

**Nit findings:**
- [phantom-todos] src/game/audio/story.ts:51 — TODO lacks ticket and owner; condition is clear. Add `(#nnn, @handle)` after `TODO:`.

### Suppressed by sidecar

This run consulted `docs/audits/_dismissals.md` and skipped 0 findings (sidecar is empty). See that file to inspect or revoke a dismissal. Do NOT add dismissals to this report — they live in the sidecar.

### Per-check breakdown (transparency)

For audit-skill transparency on this first invocation, here's what each check matched and how it was classified. Subsequent runs will only surface flagged findings; this verbose breakdown is one-time for skill validation.

| # | Check | Matches in scope | Classification |
|---|-------|------------------|----------------|
| 1 | defensive-checks-for-impossible-cases | 16+ matches (`if (!el)`, `if (!cb)`, `if (!Ctor)`, `if (!sc)`, `if (!voice)`, `if (!item)`, `if (!music)`, `if (!listenersAttached)`) | **All suppressed.** Sources are narrowing-eligible (`queue.shift()` → `T \| undefined`; `this.el` declared `HTMLAudioElement \| null`; `window.webkitAudioContext` cross-browser trust boundary; `localStorage` access; module-level boolean state guards). Per the "How to verify" sub-bullet, none are flaggable smells. |
| 2 | stylistic-drift-within-file | 0 matches | Codebase is Prettier-enforced. |
| 3 | paraphrase-comments | 0 matches in scope | Comments in `src/game/audio/` explain *why*, not *what* (per CLAUDE.md §5). One match in `sfx.test.ts` was an explanatory comment about `autoDispose` — but tests are excluded from default scope, and on inspection the comment explains rationale, not paraphrases code. |
| 4 | single-use-helpers | Not exhaustively checked (requires cross-codebase grep per the Scope rule). Spot-check: `audioBus.register`, `userActivation` event-attach, `MusicEngine.init` — all multi-use. No obvious single-use exports surfaced. |
| 5 | generic-names-in-domain-context | Several legitimate uses (`item` in queue accessors, `data` for buffer ops). All match the skip rule for utility-domain context. **0 flagged.** |
| 6 | swallowed-errors | 0 matches. Every `catch` in `src/game/audio/` either logs (`console.warn`) or has a documented why-comment (`seenStoriesLocal.ts:18` pattern). |
| 7 | mirror-tests | **Dormant** under default scope. Test files excluded. To audit tests, re-run with `--include-tests`. |
| 8 | phantom-todos | **1 match, 1 flagged** — `story.ts:51`. The constructor-block TODO has a condition but no ticket/owner. Listed above. |
| 9 | duplicated-helpers | Eyeball pass: `leaderboardAudio.ts`, `menuBriefingAudio.ts`, `storyLogAudio.ts` each define a voice-playback class but their internal state machines differ enough to fail the 1+2+3 heuristic (different parameter shapes, different body skeletons). `itemSfx.ts` has 4 cue methods (weapon/augment/upgrade/money) — each is a one-liner delegating to a shared `play()` method. Same shape but the body literal IS the contract (skip rule applies). **0 flagged.** |
| 10 | over-typed-primitives | 0 matches. No branded types or unnecessary `as const` / `satisfies` in `src/game/audio/`. |

### Per-check breakdown for the skill's own `.md` files

Most checks don't apply to prose; the audit honestly reports zero across the board.

| # | Check | Result |
|---|-------|--------|
| 1-7, 9-10 | Code-only checks | **N/A** for `.md` files (no TS/JS code to scan). |
| 8 | phantom-todos | 0 matches in `SKILL.md`, `false-positive-log.template.md`, or `_dismissals.md`. No TODO/FIXME/XXX/HACK comments in any of the three. |

The audit found **no findings in the skill's own documentation**. The skill passes its own audit (limited surface — the checks are code-targeted, and the docs are prose with no actionable smells).

**Summary: 1 nit**
