# AI-codegen smell audit — 2026-05-17

## Run 1 (04:30 UTC)

**Scope:** `src/game/audio/*.ts` (excluding `*.test.ts` per default rule) + `.claude/skills/ai-codegen-smell-audit/*.md` (user-requested inclusion of the skill's own docs).
**Checks run:** 10 (with check #7 mirror-tests dormant — default scope excludes test files; user did not pass `--include-tests`).
**Total findings:** 1 (0 major, 0 minor, 1 nit).

### Findings

| Check | Severity | File:line | Snippet | Suggested action |
|-------|----------|-----------|---------|------------------|
| phantom-todos | nit | src/game/audio/story.ts:51 | `// TODO: when per-category sliders ship in MuteToggle, this engine plays...` | Add a ticket (`#nnn`) and an owner (`@handle`) so future maintainers can track ownership. The condition is present and clear ("when per-category sliders ship") — only the ticket + owner are missing. |

### Grouped by severity

One bullet per finding. Omit a severity section entirely if it has zero findings.

**Nit findings:**
- [phantom-todos] src/game/audio/story.ts:51 — TODO lacks ticket and owner; condition is clear. Add `(#nnn, @handle)` after `TODO:`.

### Suppressed by sidecar

This run consulted `docs/audits/_dismissals.md` and skipped 0 findings (sidecar is empty). See that file to inspect or revoke a dismissal. Do NOT add dismissals to this report — they live in the sidecar.

**Summary: 1 nit**
