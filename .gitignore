# deps
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# next.js
/.next/
/out/
/build

# typescript / build artefacts
*.tsbuildinfo
next-env.d.ts

# test / coverage
/coverage
*.lcov

# turbo / nx caches
.turbo
.nx

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*
*.log

# env
.env
.env*.local
!.env.example
!.env.local.example

# vercel
.vercel

# operational backups — JSON snapshots of prevRow taken before any
# scripts/_lib/dbWriteSafety.mjs UPDATE. Contains real player save data;
# must never be committed to a public repo even by accident.
/db-backups/

# ide
.idea
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json

# os
.DS_Store
Thumbs.db
*.swp
*.pem

# dbmate
/db/schema.sql

# Claude Code worktrees and per-session settings (skills/ IS tracked)
.claude/worktrees/
.claude/settings.local.json
